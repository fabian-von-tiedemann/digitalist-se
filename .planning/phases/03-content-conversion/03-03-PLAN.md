---
phase: 03-content-conversion
plan: 03
type: execute
depends_on: []
files_modified: [src/components/ContactForm.tsx, src/components/NewsletterSignup.tsx, src/components/CTABanner.tsx, src/app/[locale]/kontakt/page.tsx, messages/sv.json, messages/en.json]
---

<objective>
Create contact page and conversion-focused components throughout the site

Purpose: Maximize lead generation with multiple conversion points - the core business goal of the site
Output: Contact page with form, newsletter signup component, CTA banners for strategic placement
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/Hero.tsx
@src/app/[locale]/page.tsx
@messages/sv.json

**Tech stack available:** Next.js 15, next-intl, Tailwind CSS, Directus SDK
**Established patterns:** Client components with useTranslations, brutalist styling, locale-aware routing
**Constraining decisions:**
- PROJECT.md: "Pengar in" - every element evaluated against conversion
- PROJECT.md: Multiple conversion points required (book meeting, contact form, whitepaper)
- Phase 01-03: Brutalist design tokens
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ContactForm component with validation</name>
  <files>src/components/ContactForm.tsx</files>
  <action>
Create ContactForm client component:
- Fields: name (required), email (required, email validation), company, phone, message (required), interest dropdown (Shield/Spear/Core/Brain/General)
- Brutalist form styling: border-2 inputs, accent focus states, shadow-brutal on button
- Client-side validation with error messages
- Submit handler that logs to console for v1 (actual API integration deferred)
- Success state with confirmation message
- Loading state during submission

Use native HTML5 validation + custom error display.
Do NOT install form libraries - keep dependencies minimal.
Focus states must be accessible (visible ring).
  </action>
  <verify>npm run build passes, form renders with all fields, validation works</verify>
  <done>ContactForm component exists with validation, brutalist styling, accessible focus states</done>
</task>

<task type="auto">
  <name>Task 2: Create NewsletterSignup and CTABanner components</name>
  <files>src/components/NewsletterSignup.tsx, src/components/CTABanner.tsx</files>
  <action>
Create NewsletterSignup component:
- Inline email input with submit button
- Compact design for footer/sidebar placement
- Success state showing confirmation
- Brutalist styling matching site aesthetic

Create CTABanner component:
- Props: variant ('primary' | 'secondary'), headline, subtext, ctaText, ctaLink
- Primary: full-width dark background, prominent CTA
- Secondary: lighter, more subtle for mid-page placement
- Both variants: brutalist borders, hover animations
- Use locale-aware Link for internal URLs

These are reusable conversion components for strategic placement throughout site.
  </action>
  <verify>npm run build passes, components render correctly</verify>
  <done>NewsletterSignup and CTABanner components exist, multiple variants, proper styling</done>
</task>

<task type="auto">
  <name>Task 3: Create contact page and add conversion points to homepage</name>
  <files>src/app/[locale]/kontakt/page.tsx, src/app/[locale]/page.tsx, messages/sv.json, messages/en.json</files>
  <action>
Create contact page at /kontakt:
- Page header with compelling headline
- Two-column layout: form on left, contact info on right
- Contact info: address, email, phone, business hours
- Map placeholder (iframe or static image deferred)
- Trust signals at bottom (ISO badges reuse)

Add conversion components to homepage:
- CTABanner (primary) after blog preview section
- NewsletterSignup in a dedicated section or integrated into footer

Add translations:
- contact.title, contact.subtitle, contact.form.* (all field labels, placeholders, errors, success)
- contact.info.* (address, email, phone, hours)
- newsletter.* (title, placeholder, button, success)
- cta.* (headline, subtext variations for different contexts)
  </action>
  <verify>/kontakt page renders with form and info, homepage has CTA banner, translations work</verify>
  <done>Contact page complete, conversion points placed strategically, all text translatable</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] /kontakt page displays with working form
- [ ] Form validation shows errors for invalid input
- [ ] Homepage has at least one CTA banner
- [ ] Newsletter signup component works
- [ ] Both sv and en translations complete
- [ ] All interactive elements have visible focus states
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- Multiple conversion points implemented
- Form validation is user-friendly and accessible
- Design matches brutalist aesthetic
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-conversion/03-03-SUMMARY.md`
</output>
