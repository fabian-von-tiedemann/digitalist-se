---
phase: 02-core-pages
plan: 03
type: execute
depends_on: ["02-01"]
files_modified: [src/components/TrustSignals.tsx, src/components/ClientLogos.tsx, src/app/[locale]/page.tsx, public/logos/, messages/sv.json, messages/en.json]
---

<objective>
Create trust signals section with ISO certifications and client logos for social proof.

Purpose: Build credibility and trust with public sector decision-makers by showcasing certifications and existing client relationships. Trust signals must appear early on page (visible with minimal scroll).
Output: TrustSignals component with certifications, ClientLogos component with logo grid, integrated into homepage.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan context:
@.planning/phases/02-core-pages/02-01-SUMMARY.md

# Source files:
@src/app/[locale]/page.tsx
@src/app/globals.css
@tailwind.config.ts
@messages/sv.json

**Tech stack available:** Next.js 15, next-intl, Tailwind CSS
**Established patterns:**
- Design tokens: primary (navy), accent (blue), concrete (gray)
- Brutalist aesthetic

**From PROJECT.md requirements:**
- Trust signals: ISO-certifikat, kundloggor, synligt tidigt pa sidan
- Kundcase-sektion med Socialstyrelsen och andra dokumenterade case

**Target clients (public sector focus):**
- Socialstyrelsen (documented case)
- Other Swedish government agencies
- Regional healthcare organizations

**Certifications to highlight:**
- ISO 27001 (Information Security)
- ISO 9001 (Quality Management)
- GDPR-compliant infrastructure

**Constraining decisions:**
- Public sector target = credibility paramount
- "Myndig Innovation" aesthetic = authoritative, not flashy
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TrustSignals certification badges</name>
  <files>src/components/TrustSignals.tsx, messages/sv.json, messages/en.json</files>
  <action>
Create TrustSignals component displaying certifications:
- Horizontal row of certification badges (flex, gap-8)
- Each badge: icon + label (ISO 27001, ISO 9001, GDPR, Swedish Hosting)
- Subtle styling: concrete-200 background, small rounded, muted text
- Icons as CSS geometric shapes (shield, checkmark, etc.) - no external dependencies

Badge content:
1. ISO 27001 - "Certifierad informationssakerhet"
2. ISO 9001 - "Certifierad kvalitetsledning"
3. GDPR - "GDPR-kompatibel"
4. Swedish flag/marker - "Svensk drift"

Layout: Centered, full-width strip, can appear above or below hero
Responsive: Wrap on mobile, single row on desktop

Add translations under "trust" key in message files.
  </action>
  <verify>TypeScript compiles, badges render in row</verify>
  <done>Four certification badges display, responsive layout works</done>
</task>

<task type="auto">
  <name>Task 2: Create ClientLogos component with placeholder grid</name>
  <files>src/components/ClientLogos.tsx, messages/sv.json, messages/en.json</files>
  <action>
Create ClientLogos component for social proof:
- Section title: "Betrodda av ledande organisationer" (text-display-sm, font-display)
- Grid of client logos (grayscale, hover to color effect)
- Placeholder logos using CSS shapes (no actual logo files yet):
  - Socialstyrelsen (placeholder)
  - Swedish government agency placeholder x3
  - Healthcare organization placeholder x2

Styling:
- Logos in grayscale (filter: grayscale(100%))
- Hover: transition to full color
- Grid: 6 columns desktop, 3 tablet, 2 mobile
- Each logo box: consistent aspect ratio, centered content
- Subtle border or background to define logo areas

Note: Use placeholder divs with organization names as text. Actual logo files will be added in Phase 3 or later when assets are provided.

Structure:
```tsx
<section>
  <h2>Betrodda av ledande organisationer</h2>
  <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-8">
    {logos.map(logo => <LogoPlaceholder key={logo.name} {...logo} />)}
  </div>
</section>
```
  </action>
  <verify>npm run build succeeds, logo grid renders</verify>
  <done>Logo grid displays with 6 placeholder positions, responsive</done>
</task>

<task type="auto">
  <name>Task 3: Integrate trust signals into homepage</name>
  <files>src/app/[locale]/page.tsx</files>
  <action>
Update homepage to include trust sections:
1. Add TrustSignals immediately after Hero (or within Hero at bottom)
2. Add ClientLogos after Dimensions section
3. Ensure visual rhythm with alternating backgrounds

Page structure:
```tsx
<>
  <Hero />
  <TrustSignals />  {/* Certification badges - visible early */}
  <Dimensions />
  <ClientLogos />   {/* Social proof - after service offerings */}
</>
```

TrustSignals positioning: Should be visible within first viewport or immediately after scroll. This is critical for public sector credibility.

Add appropriate section padding and background contrast.
  </action>
  <verify>npm run dev shows all sections in correct order</verify>
  <done>Homepage displays Hero -> TrustSignals -> Dimensions -> ClientLogos</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run dev` shows trust signals visible early on page
- [ ] Certification badges render in horizontal row
- [ ] Client logo grid is responsive (6/3/2 columns)
- [ ] Swedish and English translations work
- [ ] Visual hierarchy supports "credibility first" goal
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- Trust signals visible within first viewport or minimal scroll
- Client logos display as professional placeholder grid
- Phase 2 complete - core pages functional
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages/02-03-SUMMARY.md`
</output>
