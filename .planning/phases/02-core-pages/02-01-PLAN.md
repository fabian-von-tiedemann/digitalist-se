---
phase: 02-core-pages
plan: 01
type: execute
depends_on: []
files_modified: [src/components/Header.tsx, src/components/Hero.tsx, src/app/[locale]/page.tsx, src/app/[locale]/layout.tsx, messages/sv.json, messages/en.json]
---

<objective>
Create the hero section and navigation for digitalist.se homepage.

Purpose: Establish the visual identity and primary conversion pathway. The hero must communicate "Suveran AI for offentlig sektor" within 5 seconds and provide clear CTA for lead generation.
Output: Header component with navigation, Hero component with headline/CTA, updated homepage integrating both.
</objective>

<execution_context>
./.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase 1 established patterns:
@.planning/phases/01-foundation/01-03-SUMMARY.md

# Current source files:
@src/app/[locale]/page.tsx
@src/app/[locale]/layout.tsx
@src/app/globals.css
@tailwind.config.ts
@messages/sv.json

**Tech stack available:** Next.js 15, next-intl, Tailwind CSS
**Established patterns:**
- Locale-aware pages in src/app/[locale]/
- Message files in messages/{locale}.json
- Design tokens: primary (navy), accent (blue), concrete (gray)
- Brutalist aesthetic with sharp edges, shadow animations
- Fluid typography with clamp()

**Constraining decisions:**
- Phase 01-03: Three-palette colors (primary/accent/concrete)
- Phase 01-01: next-intl for translations with Swedish default
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Header component with navigation</name>
  <files>src/components/Header.tsx, messages/sv.json, messages/en.json</files>
  <action>
Create responsive Header component:
- Logo/brand mark left (text "Digitalist" in font-display)
- Main navigation links: Tjanster, Om oss, Kontakt (use next-intl Link component)
- Mobile hamburger menu with slide-out drawer (use Tailwind transitions)
- CTA button "Boka mote" (primary style, btn-brutalist class)
- Sticky header with backdrop blur on scroll (use CSS sticky + backdrop-blur)
- Use useTranslations hook for all text
- Add nav translations to messages/sv.json and messages/en.json

Navigation structure:
```
nav:
  services: "Tjanster"
  about: "Om oss"
  contact: "Kontakt"
  cta: "Boka mote"
```

Do NOT use any UI library - pure Tailwind. Keep mobile menu state with useState.
  </action>
  <verify>npm run build succeeds, Header renders at localhost:3000</verify>
  <done>Header sticky, navigation links present, mobile menu toggles, CTA visible</done>
</task>

<task type="auto">
  <name>Task 2: Create Hero component with value proposition</name>
  <files>src/components/Hero.tsx, messages/sv.json, messages/en.json</files>
  <action>
Create Hero component for homepage:
- Headline: "Suveran AI for offentlig sektor" (text-display-xl, font-display)
- Subheadline: "Sveriges enda oppna helhetspartner for AI som foljer GDPR, OSL och AI Act" (text-xl, max-w-2xl)
- Primary CTA: "Boka ett mote" (btn-brutalist, bg-accent-500, text-white)
- Secondary CTA: "Las mer om vara losningar" (text link with arrow)
- Visual element: Abstract geometric pattern or gradient suggesting technology/authority (CSS-only, no images)
- Full viewport height hero (min-h-screen) with content centered
- Use design tokens from tailwind.config.ts (primary-900, accent-500, etc.)

Translations to add:
```
hero:
  headline: "Suveran AI for offentlig sektor"
  subheadline: "Sveriges enda oppna helhetspartner for AI som foljer GDPR, OSL och AI Act"
  cta_primary: "Boka ett mote"
  cta_secondary: "Las mer om vara losningar"
```

Brutalist styling: Sharp edges, strong contrast, authoritative feel matching "Myndig Innovation".
  </action>
  <verify>npm run build succeeds, Hero renders with correct typography scale</verify>
  <done>Hero fills viewport, headline readable within 5 seconds, CTAs clickable</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Header and Hero into homepage</name>
  <files>src/app/[locale]/page.tsx, src/app/[locale]/layout.tsx</files>
  <action>
Update layout and page to include new components:
1. Import Header into layout.tsx, render above {children}
2. Replace current page.tsx content with Hero component
3. Ensure Header is outside main content flow (fixed/sticky positioning)
4. Add proper semantic HTML: header element for Header, main for content, section for Hero
5. Test both Swedish (/) and English (/en) routes render correctly

Structure:
```tsx
// layout.tsx
<body>
  <Header />
  <main>{children}</main>
</body>

// page.tsx
<Hero />
```
  </action>
  <verify>npm run dev shows Header + Hero, both / and /en routes work</verify>
  <done>Homepage displays Header with navigation and Hero section, translations work in both languages</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run dev` shows homepage with Header and Hero
- [ ] Navigation links are visible and styled
- [ ] Mobile menu toggles correctly
- [ ] Hero headline visible within 5 seconds
- [ ] CTAs are clickable with correct styling
- [ ] Swedish (/) and English (/en) routes both render correctly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No TypeScript errors
- Header and Hero match "Myndig Innovation" aesthetic
- Translations work for both locales
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages/02-01-SUMMARY.md`
</output>
